import React from 'react';

import { StyledResizerComponent } from '../StyledTableComponents';

const ResizerComponent = props => <StyledResizerComponent {...props} />;

export default ResizerComponent;
